{
  "extends": [
    "//"
  ],
  "tasks": {
    "build": {
      "inputs": [
        "$TURBO_DEFAULT$",
        ".env*",
        "astro.config.*",
        "package.json",
        "postcss.config.*",
        "tailwind.config.*",
        "tsconfig*.json"
      ],
      "outputs": [
        "dist/**"
      ]
    }
  }
}
