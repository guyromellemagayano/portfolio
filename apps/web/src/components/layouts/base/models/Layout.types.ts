import type { DivProps, DivRef } from "@guyromellemagayano/components";

/** Ref for the `Layout` component. */
export type LayoutRef = DivRef;

/** Props for the `Layout` component. */
export interface LayoutProps extends DivProps {}
