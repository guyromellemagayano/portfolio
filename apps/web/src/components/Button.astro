---
/**
 * @file apps/web/src/components/Button.astro
 * @author Guy Romelle Magayano
 * @description Button primitive for actions and form submission.
 */

const {
  size = "md",
  type = "button",
  color = "dark/light",
  class: className = "",
  ...rest
} = Astro.props;

const sizeClass = size === "lg" ? "px-4 py-2" : "px-3 py-1";
---

<button
  {...rest}
  type={type}
  class:list={[
    "inline-flex shrink-0 items-center justify-center gap-1 rounded-full text-sm/7 font-medium transition",
    color === "dark/light" &&
      "bg-button text-white hover:bg-button-hover dark:bg-button dark:text-white dark:hover:bg-button-hover",
    color === "light" &&
      "bg-button text-white hover:bg-button-hover dark:bg-button dark:text-white dark:hover:bg-button-hover",
    sizeClass,
    className,
  ]}
>
  <slot />
</button>
