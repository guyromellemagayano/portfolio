---
/**
 * @file apps/web/src/components/HeroSimpleLeftAligned.astro
 * @author Guy Romelle Magayano
 * @description Hero section with left-aligned copy and action links.
 */

import { Section as UiSection } from "@portfolio/ui";
import Container from "@web/components/Container.astro";
import Text from "@web/components/Text.astro";

const { class: className = "", ...rest } = Astro.props;
const hasSubheadline = Astro.slots.has("subheadline");
const hasFooter = Astro.slots.has("footer");
const sectionClass = ["py-16", className].filter(Boolean).join(" ");
---

<UiSection {...rest} className={sectionClass}>
  <Container class="flex flex-col items-start gap-6">
    <slot name="eyebrow" />
    <slot name="headline" />
    {
      hasSubheadline ? (
        <Text size="lg" class="flex max-w-xl flex-col gap-4">
          <slot name="subheadline" />
        </Text>
      ) : null
    }
    <slot name="cta" />
    {
      hasFooter ? (
        <div class="pt-4">
          <slot name="footer" />
        </div>
      ) : null
    }
  </Container>
</UiSection>
