---
/**
 * @file apps/web/src/pages/labs/[slug].astro
 * @author Guy Romelle Magayano
 * @description Lab detail page for an individual experiment.
 */

import CTA from "@web/components/CTA.astro";
import ProjectDetailHeader from "@web/components/ProjectDetailHeader.astro";
import SplitSection from "@web/components/SplitSection.astro";
import { getProjectPath, labProjects, type Project } from "@web/data/projects";
import BaseLayout from "@web/layouts/BaseLayout.astro";
import {
  buildOpenGraphImages,
  buildPageMetadata,
  buildTwitterImages,
  resolveMetadataDescription,
  resolveMetadataTitle,
} from "@web/lib/metadata";
import { type WebPageMetadata } from "@web/lib/metadata.types";
import {
  buildBreadcrumbListStructuredData,
  buildProjectStructuredData,
} from "@web/lib/structured-data";

export function getStaticPaths() {
  return labProjects.map((project) => ({
    params: {
      slug: project.slug,
    },
  }));
}

const slug = Astro.params.slug ?? "";
const project = labProjects.find((entry) => entry.slug === slug);

if (!project) {
  return Astro.redirect("/404", 404);
}

function buildProjectMetadata(projectRecord: Project): WebPageMetadata {
  const projectPath = getProjectPath(projectRecord);
  const title = `${projectRecord.title} Lab`;
  const description = resolveMetadataDescription(
    projectRecord.caseStudy.outcome,
    projectRecord.description,
    projectRecord.title
  );

  return buildPageMetadata({
    canonicalPathOrUrl: projectPath,
    description,
    openGraphImages: buildOpenGraphImages({
      alt: `${projectRecord.title} lab by Guy Romelle Magayano`,
    }),
    structuredData: [
      buildProjectStructuredData(projectRecord),
      buildBreadcrumbListStructuredData([
        { name: "Home", path: "/" },
        { name: "Labs", path: "/labs" },
        {
          name: projectRecord.title,
          path: projectPath,
        },
      ]),
    ],
    title: resolveMetadataTitle(title, projectRecord.title),
    twitterImages: buildTwitterImages(),
  });
}

const metadata = buildProjectMetadata(project);
---

<BaseLayout metadata={metadata}>
  <article>
    <ProjectDetailHeader
      backHref="/labs"
      backLabel="Back to labs"
      project={project}
    />

    <SplitSection
      title="Problem"
      intro="The product or system question the lab was meant to make concrete."
      sectionClass="mt-20"
    >
      <p class="text-sm leading-7 text-zinc-600 dark:text-zinc-300">
        {project.caseStudy.problem}
      </p>
    </SplitSection>

    <SplitSection
      title="Role"
      intro="How I used the lab to test architecture, UX tradeoffs, or product modeling."
      sectionClass="mt-20"
    >
      <p class="text-sm leading-7 text-zinc-600 dark:text-zinc-300">
        {project.caseStudy.role}
      </p>
    </SplitSection>

    <SplitSection
      title="Decisions"
      intro="The product-system choices that make the experiment useful beyond the demo itself."
      sectionClass="mt-20"
    >
      <div class="space-y-6">
        {
          project.caseStudy.decisions.map((decision) => (
            <p class="text-sm leading-7 text-zinc-600 dark:text-zinc-300">
              {decision}
            </p>
          ))
        }
      </div>
    </SplitSection>

    <SplitSection
      title="Outcome"
      intro="What the lab clarified or proved once the model was built."
      sectionClass="mt-20"
    >
      <p class="text-sm leading-7 text-zinc-600 dark:text-zinc-300">
        {project.caseStudy.outcome}
      </p>
    </SplitSection>

    <SplitSection
      title="Proof points"
      intro="Evidence that the experiment surfaced durable implementation ideas."
      sectionClass="mt-20"
    >
      <ul class="space-y-3 text-sm leading-7 text-zinc-600 dark:text-zinc-300">
        {project.caseStudy.proof.map((proofPoint) => <li>{proofPoint}</li>)}
      </ul>
    </SplitSection>
  </article>

  <CTA
    eyebrow="Want the production version?"
    title="Turn the lab into a real system conversation."
    body="Labs show the product model and engineering judgment. Production work starts by mapping those choices against your real constraints, team, and release path."
    primaryHref="/contact"
    primaryLabel="Start a conversation"
    secondaryHref="/work"
    secondaryLabel="View case studies"
  />
</BaseLayout>
