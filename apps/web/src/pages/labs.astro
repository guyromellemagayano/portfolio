---
import Container from "@web/components/Container.astro";
import PageIntro from "@web/components/PageIntro.astro";
import PathwayCards from "@web/components/PathwayCards.astro";
import ProjectCard from "@web/components/ProjectCard.astro";
import BaseLayout from "@web/layouts/BaseLayout.astro";
import { getProjectPath, labProjects } from "@web/data/projects";
import { pagePathways } from "@web/data/site";
import {
  buildPortfolioPageMetadata,
  getPortfolioBrochurePage,
} from "@web/lib/portfolio-brochure";
import {
  appendStructuredData,
  buildCollectionPageStructuredData,
} from "@web/lib/structured-data";

const { page } = await getPortfolioBrochurePage("labs");
const metadata = appendStructuredData(
  buildPortfolioPageMetadata(page),
  buildCollectionPageStructuredData(page, labProjects, getProjectPath)
);
---

<BaseLayout metadata={metadata}>
  <PageIntro
    subheading={page.subheading}
    title={page.title}
    intro={page.intro}
  />

  <section aria-labelledby="labs-showcase-heading" role="region">
    <Container class="py-16">
      <h2
        id="labs-showcase-heading"
        class="max-w-2xl text-2xl font-semibold tracking-tight text-zinc-800 sm:text-3xl dark:text-zinc-100"
      >
        Product-system experiments, reference builds, and internal proofs of
        concept.
      </h2>
      <div class="mt-10 grid gap-x-12 gap-y-16 sm:grid-cols-2 lg:grid-cols-3">
        {labProjects.map((project) => <ProjectCard project={project} />)}
      </div>
    </Container>
  </section>

  <PathwayCards
    eyebrow="Next"
    id="labs-next-steps-heading"
    title="Move from experiment to production context."
    intro="Labs show product-system thinking; production work and services show how that thinking maps to real constraints."
    pathways={pagePathways.labs}
    sectionClass="pb-24"
  />
</BaseLayout>
